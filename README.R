library(xlsx)
order <- read.xlsx("Global Superstore.xls", sheetIndex = 1, header = TRUE)

"The data reading process takes sooo long i completed 3 course videos while
waiting. Can my laptop even bear reading another sheet? The RAM is already 88%
used."

names(order)
